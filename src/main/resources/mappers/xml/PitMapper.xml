<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzm.Fitpin.mapper.PitMapper">

    <!-- 상의 수선 정보 삽입 쿼리 -->
    <insert id="insertPitTop" parameterType="com.mzm.Fitpin.dto.PitTopDTO">
        INSERT INTO pitTop (itemKey, cartKey, itemSize, itemHeight, itemShoulder, itemChest, itemSleeve)
        VALUES (#{itemKey}, #{cartKey}, #{itemSize}, #{itemHeight}, #{itemShoulder}, #{itemChest}, #{itemSleeve})
    </insert>

    <!-- 하의 수선 정보 삽입 쿼리 -->
    <insert id="insertPitBottom" parameterType="com.mzm.Fitpin.dto.PitBottomDTO">
        INSERT INTO pitBottom (itemKey, cartKey, itemSize, itemHeight, itemWaists, itemThighs, frontrise, itemHemWidth)
        VALUES (#{itemKey}, #{cartKey}, #{itemSize}, #{itemHeight}, #{itemWaists}, #{itemThighs}, #{frontrise}, #{itemHemWidth})
    </insert>


    <select id="findPitTopByCartKey" resultType="com.mzm.Fitpin.dto.PitTopDTO">
        SELECT *
        FROM pitTop
        WHERE cartKey = #{cartKey}
    </select>

    <select id="findPitBottomByCartKey" resultType="com.mzm.Fitpin.dto.PitBottomDTO">
        SELECT *
        FROM pitBottom
        WHERE cartKey = #{cartKey}
    </select>

</mapper>
