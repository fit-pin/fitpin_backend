<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzm.Fitpin.mapper.OrderMapper">

    <insert id="insertOrder" parameterType="com.mzm.Fitpin.post.OrderPostRequest">
        INSERT INTO `order` (itemKey, userName, userAddr, userNumber, itemName, itemImg1, itemSize, itemPrice, itemBuycnt, itemTotal, pitPrice)
        VALUES (#{itemKey}, #{userName}, #{userAddr}, #{userNumber}, #{itemName}, #{itemImg1}, #{itemSize}, #{itemPrice}, #{itemBuycnt}, #{itemTotal}, #{pitPrice})
    </insert>


</mapper>
