<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mzm.Fitpin.mapper.order.OrderMapper">

    <insert id="insertOrder">
        INSERT INTO orderList (
            itemKey, userKey, userName, userAddr, userNumber, itemName, itemImg,
            itemSize, itemPrice, itemTotal, pitPrice, pcs)
        VALUES (
                   #{itemKey}, #{userKey}, #{userName}, #{userAddr}, #{userNumber}, #{itemName}, #{itemImg},
                   #{itemSize}, #{itemPrice}, #{itemTotal}, #{pitPrice}, #{qty}
               )
    </insert>

    <select id="getOrderByUserKey" resultType="com.mzm.Fitpin.dto.order.OrderDTO">
        SELECT *
        FROM orderList
        WHERE userKey = #{userKey}
    </select>

</mapper>
